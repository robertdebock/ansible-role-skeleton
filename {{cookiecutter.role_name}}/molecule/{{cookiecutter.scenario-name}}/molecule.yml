---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: {{ cookiecutter.role_name }}-{{ cookiecutter.scenario_name }}
    image: centos:latest
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: {{ cookiecutter.scenario_name }}
verifier:
  name: goss
  lint:
    name: yamllint
